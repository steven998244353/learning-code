#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int T;
    cin>>T;
    cin.ignore();
    while(T--){
        string line;
        getline(cin,line);
        size_t pos = line.find(' ');
        if(line.substr(0,pos) == "Colin" || line.substr(0,pos) == "Eva"){
            cout<<"Congratulations!"<<endl;
        }
        else{
            cout<<"What?"<<endl;
        }

    }
    return 0;
}
